package application;

public enum PlayerAction {
	jab, kick, poisondart, chop, heal, block, invalid // make attacks be (jab .9 acc, kick .75 acc 1.2 damage?, poison
														// dart .5 damage,
	// poison, 100acc?)
	// chop (.5 acc, para .8 damage)
}
